# AiStock 组件库使用指南

> 供项目开发者查阅的组件参考手册
> 技术栈：Vue 3 + TypeScript + SCSS（rpx 单位，uni-app 组件规范）

---

## 目录

1. [快速开始](#1-快速开始)
2. [基础 UI 组件](#2-基础-ui-组件)
3. [反馈组件](#3-反馈组件)
4. [金融业务组件](#4-金融业务组件)
5. [表单扩展组件](#5-表单扩展组件)
6. [覆盖层组件](#6-覆盖层组件)
7. [通用图表组件](#7-通用图表组件)
8. [通用 UI 扩展组件](#8-通用-ui-扩展组件)
9. [Web 端布局组件](#9-web-端布局组件)
10. [App 端导航组件](#10-app-端导航组件)
11. [金融业务深组件](#11-金融业务深组件)
12. [导航扩展组件](#12-导航扩展组件)
13. [AI 交互组件](#13-ai-交互组件)
14. [音频与播客组件](#14-音频与播客组件)
15. [事件与洞察组件](#15-事件与洞察组件)
16. [高级行情组件](#16-高级行情组件)
17. [辅助组件](#17-辅助组件)
18. [Design Token 变量速查](#18-design-token-变量速查)
19. [常见问题](#19-常见问题)

---

## 1. 快速开始

### 安装

```bash
pnpm install aistock-component-lib
```

### 全局注册

```ts
// main.ts
import { createApp } from 'vue'
import App from './App.vue'
import * as AsComponents from 'aistock-component-lib'

const app = createApp(App)
// 批量注册
for (const [name, component] of Object.entries(AsComponents)) {
  app.component(name, component as never)
}
app.mount('#app')
```

### 按需引入

```vue
<script setup lang="ts">
import { Button, Card, StockItem } from 'aistock-component-lib'
</script>
```

### 组件总览

| 层级 | 数量 | 组件列表 |
|------|------|---------|
| 基础 UI | 8 | Button, Card, Tag, Badge, Input, Switch, SvgIcon, Avatar |
| 反馈 | 4 | Empty, Progress, Skeleton, LoadingState |
| 金融业务 | 5 | StockItem, StatCard, ListCell, PageCard, SubPageCard |
| 表单扩展 | 6 | Checkbox, Radio, Select, Slider, Rate, Tooltip |
| 覆盖层 | 5 | Modal, Drawer, Popover, Toast, Alert |
| 通用图表 | 3 | Gauge, Sparkline, IndexCard |
| 通用 UI 扩展 | 5 | Collapse, Pagination, Timeline, Result, PeriodSelector |
| Web 端布局 | 5 | PageHeader, Toolbar, FilterBar, DataGrid, CommandPalette |
| App 端导航 | 6 | NavBar, TabBar, BottomSheet, ActionSheet, GlobalChatBar, PageShell |
| 金融业务深组件 | 9 | QuoteHeader, Ticker, AnnounceCard, DataFreshness, Heatmap, FundFlow, Watchlist, RadarChart, RelationGraph |
| 导航扩展 | 5 | Breadcrumb, Divider, Steps, Segmented, Dropdown |
| AI 交互 | 4 | StreamingText, ChatBubble, AiEventReport, AiGraph |
| 音频与播客 | 3 | AudioPlayer, PodcastCard, FloatingPodcast |
| 事件与洞察 | 6 | ImportanceStars, EventItemCard, EventHeadlineCard, InsightListCard, InsightAlertCard, GuideCard |
| 洞见标志组件 | 2 | InsightTag, InsightCard |
| 高级行情 | 7 | StockChart, TrendKlineCompare, TrendDimCard, StockDetailTable, StockCardList, StockIntelList, WindLeaderPanel |
| 辅助组件 | 6 | LoginQrCode, Analytics, CycleSelect, NewsSlider, HotBurstPanel, MarketOverview |
| **合计** | **89** | |

---

## 2. 基础 UI 组件

### Button 按钮

```vue
<Button type="primary" size="md" @click="handleClick">确认</Button>
```

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| type | `primary \| secondary \| ghost \| accent \| gold \| danger` | `primary` | 按钮样式 |
| size | `sm \| md \| lg` | `md` | 按钮尺寸 |
| disabled | `boolean` | `false` | 禁用状态 |
| loading | `boolean` | `false` | 加载中状态 |

| Event | 参数 | 说明 |
|-------|------|------|
| click | `(event: MouseEvent)` | 点击事件 |

### Card 卡片

```vue
<Card title="趋势股评分" subtitle="技术面分析" hoverable>
  <p>卡片内容</p>
  <template #footer>底部内容</template>
</Card>
```

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| title | `string` | `''` | 标题 |
| subtitle | `string` | `''` | 副标题 |
| hoverable | `boolean` | `false` | 悬浮效果 |
| clickable | `boolean` | `false` | 可点击 |

| Slot | 说明 |
|------|------|
| default | 卡片内容 |
| extra | 标题右侧附加区域 |
| footer | 底部区域 |

### Tag 标签

```vue
<Tag type="up">涨幅</Tag>
<Tag type="gold" size="lg">VIP</Tag>
```

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| type | `up \| down \| neutral \| warning \| gold` | `neutral` | 颜色类型 |
| size | `sm \| md \| lg` | `md` | 尺寸 |

### Badge 徽章

```vue
<Badge type="danger" dot />
<Badge type="gold">VIP</Badge>
```

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| type | `primary \| success \| warning \| danger \| gold \| info` | `primary` | 颜色类型 |
| size | `sm \| md \| lg` | `md` | 尺寸 |
| dot | `boolean` | `false` | 显示为圆点 |

### Input 输入框

```vue
<Input v-model="text" placeholder="输入股票代码" search-icon clearable />
```

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| modelValue | `string \| number` | `''` | 绑定值（v-model） |
| placeholder | `string` | `''` | 占位文字 |
| type | `text \| number \| password \| digit` | `text` | 输入类型 |
| disabled | `boolean` | `false` | 禁用 |
| clearable | `boolean` | `false` | 可清空 |
| searchIcon | `boolean` | `false` | 显示搜索图标 |

| Event | 说明 |
|-------|------|
| update:modelValue | 值变化 |
| focus | 聚焦 |
| blur | 失焦 |
| clear | 清空 |

### Switch 开关

```vue
<Switch v-model="enabled" />
```

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| modelValue | `boolean` | `false` | 开关状态（v-model） |
| disabled | `boolean` | `false` | 禁用 |

### SvgIcon 图标

```vue
<SvgIcon name="bell-line" size="36rpx" color="#0b5fff" />
```

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| name | `string` | - | 图标名称 |
| size | `string` | `36rpx` | 尺寸 |
| color | `string` | `#4b5a7a` | 颜色 |

内置图标：`bell-line`, `search`, `arrow-left`, `arrow-right`, `check`, `close`, `plus`, `minus`, `chevron-down`, `chevron-up`, `chevron-right`, `star`, `trending-up`, `trending-down`, `settings`

### Avatar 头像

```vue
<Avatar text="尹辰" size="md" variant="primary" />
<Avatar src="/path/to/avatar.jpg" size="lg" />
```

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| src | `string` | `''` | 图片地址（优先于文字） |
| text | `string` | `''` | 头像文字（取前 2 字） |
| size | `xs \| sm \| md \| lg` | `md` | 尺寸 |
| variant | `primary \| gold \| accent \| neutral` | `primary` | 背景色 |

---

## 3. 反馈组件

### Empty 空状态

```vue
<Empty title="暂无搜索结果" description="尝试更换关键词" />
```

| Slot | 说明 |
|------|------|
| icon | 自定义图标 |
| default | 底部操作区 |

### Progress 进度条

```vue
<!-- 线性 -->
<Progress :value="68" label="数据同步" status="primary" />
<!-- 环形 -->
<Progress :value="85" type="circle" status="danger" />
```

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| value | `number` | - | 进度值（0-100） |
| type | `line \| circle` | `line` | 类型 |
| status | `primary \| success \| warning \| danger \| gold` | `primary` | 颜色 |
| label | `string` | `''` | 标签文字 |
| size | `number` | `160` | 环形尺寸（rpx） |

### Skeleton 骨架屏

```vue
<Skeleton variant="title" />
<Skeleton variant="text" width="60%" />
<Skeleton variant="circle" />
<Skeleton variant="block" />
```

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| variant | `text \| title \| circle \| block \| card` | `text` | 变体 |
| width | `string` | - | 自定义宽度 |
| height | `string` | - | 自定义高度 |

### LoadingState 加载中

```vue
<LoadingState text="加载中..." size="md" layout="vertical" />
```

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| text | `string` | `加载中...` | 提示文字 |
| size | `sm \| md \| lg` | `md` | 尺寸 |
| layout | `horizontal \| vertical` | `horizontal` | 布局方向 |

---

## 4. 金融业务组件

### StockItem 股票项

```vue
<StockItem name="贵州茅台" code="600519.SH" :price="1689.50" :change="12.30" :change-percent="0.73" />
```

| Prop | 类型 | 说明 |
|------|------|------|
| name | `string` | 股票名称 |
| code | `string` | 股票代码 |
| price | `number` | 当前价格 |
| change | `number` | 涨跌额 |
| changePercent | `number` | 涨跌幅（%） |
| active | `boolean` | 选中高亮 |

> A 股规范：红涨绿跌（$up / $down）

### StatCard 统计卡片

```vue
<StatCard label="今日成交额" :value="12345678" trend="+5.2%" trend-type="up" />
```

| Prop | 类型 | 说明 |
|------|------|------|
| label | `string` | 标签 |
| value | `string \| number` | 数值 |
| trend | `string` | 趋势文字 |
| trendType | `up \| down \| flat` | 趋势颜色 |
| valueType | `default \| up \| down \| gold \| primary` | 数值颜色 |
| description | `string` | 描述文字 |

### ListCell 列表行

```vue
<ListCell title="持仓收益" value="+1,234.56" show-arrow clickable @click="handleClick" />
```

| Prop | 类型 | 说明 |
|------|------|------|
| title | `string` | 左侧标题 |
| value | `string` | 右侧值 |
| description | `string` | 描述 |
| showArrow | `boolean` | 显示右箭头 |
| clickable | `boolean` | 可点击 |
| border | `boolean` | 显示分隔线 |

### PageCard 页面级容器

```vue
<PageCard title="自选股" show-back @back="handleBack">
  <!-- 页面内容 -->
  <template #footer>
    <Button type="primary">添加自选</Button>
  </template>
</PageCard>
```

### SubPageCard 子页面容器

```vue
<SubPageCard title="股票详情" show-back @back="handleBack">
  <!-- 子页面内容 -->
</SubPageCard>
```

---

## 5. 表单扩展组件

### Checkbox 复选框

```vue
<Checkbox v-model="checked" label="沪深A股" />
```

### Radio 单选框

```vue
<Radio v-model="sort" value="asc" label="按涨幅" />
<Radio v-model="sort" value="desc" label="按跌幅" />
```

### Select 选择器

```vue
<Select v-model="period" :options="periodOptions" placeholder="选择周期" />
```

```ts
const periodOptions = [
  { label: '今日', value: 'today' },
  { label: '本周', value: 'week' },
  { label: '本月', value: 'month' },
]
```

### Slider 滑块

```vue
<Slider v-model="range" :min="0" :max="50" label="市盈率范围" status="primary" />
```

### Rate 评分

```vue
<Rate v-model="score" :max="5" show-text />
```

### Tooltip 提示

```vue
<Tooltip content="PEG = 市盈率 / 盈利增长率" placement="top">
  <text>PE 18.5</text>
</Tooltip>
```

---

## 6. 覆盖层组件

### Modal 模态框

```vue
<Modal v-model="visible" title="买入确认" size="md" :mask-closable="false">
  <p>确认买入 600519 × 100股</p>
  <template #footer>
    <Button type="ghost" @click="visible = false">取消</Button>
    <Button type="primary" @click="confirm">确认</Button>
  </template>
</Modal>
```

### Drawer 抽屉

```vue
<Drawer v-model="visible" title="筛选条件" placement="right">
  <!-- 筛选内容 -->
  <template #footer>
    <Button type="primary">应用筛选</Button>
  </template>
</Drawer>
```

| placement | 说明 |
|-----------|------|
| `left` | 左侧滑出 |
| `right` | 右侧滑出 |
| `bottom` | 底部弹出 |

### Popover 气泡卡片

```vue
<Popover title="说明" content="这是提示内容" placement="bottom">
  <Button type="secondary" size="sm">点击查看</Button>
</Popover>
```

### Toast 轻提示

```vue
<Toast v-if="toastVisible" message="操作成功" type="success" :duration="2000" @close="toastVisible = false" />
```

| type | 说明 |
|------|------|
| `success` | 成功（绿色） |
| `error` | 错误（红色） |
| `warning` | 警告（橙色） |
| `info` | 信息（蓝色） |

### Alert 提示框

```vue
<Alert type="warning" title="风险提示" description="该操作存在较高风险" closable />
```

---

## 7. 通用图表组件

### Gauge 仪表盘

```vue
<Gauge :value="76" :max="100" label="综合评分" status="gold" />
```

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| value | `number` | - | 当前值 |
| max | `number` | `100` | 最大值 |
| size | `number` | `200` | 尺寸（rpx） |
| label | `string` | `''` | 底部标签 |
| unit | `string` | `''` | 数值单位 |
| status | `primary \| success \| warning \| danger \| gold` | `primary` | 颜色 |

### Sparkline 迷你走势图

```vue
<Sparkline :data="[10, 15, 8, 20, 18, 25, 22]" trend="up" height="60rpx" />
```

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| data | `number[]` | - | 数据点 |
| trend | `up \| down \| flat` | `flat` | 趋势颜色 |
| width | `string` | `100%` | 宽度 |
| height | `string` | `60rpx` | 高度 |

### IndexCard 指数卡片

```vue
<IndexCard name="上证指数" code="000001.SH" :value="3289.45" :change="15.32" :change-percent="0.47" trend="up" />
```

---

## 8. 通用 UI 扩展组件

### Collapse 折叠面板

```vue
<Collapse v-model:open="open" title="公告详情">此处为折叠内容…</Collapse>
```
Props：`title?`、`open?`（v-model:open，默认 false）、`disabled?`。Events：`update:open`、`change`。

### Pagination 分页

```vue
<Pagination v-model:current="page" :total="85" :page-size="10" @change="onPage" />
```
Props：`current?`（v-model，默认 1）、`total`、`pageSize?`（默认 10）、`prevText?`、`nextText?`。Events：`update:current`、`change`。

### Timeline 时间线

```vue
<Timeline :items="[{ time:'2026-08-01', title:'半年报披露', status:'success' }]" mode="left" />
```
Props：`items: { time; title; desc?; status?: 'primary'|'success'|'warning'|'danger' }[]`、`mode?: 'left'|'right'|'alternate'`（默认 left）。

### Result 结果页

```vue
<Result status="success" title="买入成功" subtitle="已成交 100 股">
  <Button type="primary">查看持仓</Button>
</Result>
```
Props：`status?: 'success'|'warning'|'error'|'info'`、`title?`、`subtitle?`。Slot：default（操作区）、icon。

### PeriodSelector 周期选择器

```vue
<PeriodSelector v-model:value="idx" :periods="['分时','日K','周K']" @change="onPeriod" />
```
Props：`periods?`（默认 `['分时','5日','日K','周K','月K']`）、`value?`（v-model，默认 0）。Events：`update:value`、`change`。

---

## 9. Web 端布局组件

### PageHeader 页头

```vue
<PageHeader title="贵州茅台" subtitle="600519.SH" back @back="goBack">
  <template #extra><Button size="sm">关注</Button></template>
</PageHeader>
```
Props：`title?`、`subtitle?`、`back?`、`transparent?`。Slot：extra、default。Events：`back`。

### Toolbar 工具栏

```vue
<Toolbar title="自选股" sticky>
  <Button size="sm" type="ghost">编辑</Button>
  <template #right><Button size="sm">排序</Button></template>
</Toolbar>
```
Props：`title?`、`sticky?`。Slot：default、right。

### FilterBar 筛选栏

```vue
<FilterBar :active-count="3" @reset="onReset" @confirm="onConfirm">
  <Tag>消费</Tag><Tag>科技</Tag>
</FilterBar>
```
Props：`activeCount?`（默认 0）。Slot：default。Events：`reset`、`confirm`。

### DataGrid 高级数据表格

```vue
<DataGrid :columns="cols" :data="rows" stripe fixed-header @row-click="onRow">
  <template #changePct="{ row }"><span :style="{color: row.changePct>=0?'#e54d5e':'#18a058'}">{{ row.changePct }}%</span></template>
</DataGrid>
```
Props：`columns: { key; title; width?; align? }[]`、`data: Record<string, unknown>[]`、`rowKey?`、`stripe?`、`fixedHeader?`。每列可用具名 slot（slot 名 = key，scoped `{ row, index }`）。Events：`row-click`。

### CommandPalette 命令面板

```vue
<CommandPalette v-model:open="open" :commands="cmds" placeholder="搜索命令…" @select="onSelect" />
```
Props：`open?`（v-model，默认 false）、`commands: { key; title; subtitle?; icon? }[]`、`placeholder?`。Events：`update:open`、`select`。支持上下键高亮、回车确认、ESC 关闭。

---

## 10. App 端导航组件

### NavBar 顶部导航栏

```vue
<NavBar title="股票详情" back @back="goBack">
  <template #right><SvgIcon name="search" size="36rpx" /></template>
</NavBar>
```
Props：`title?`、`back?`、`leftText?`、`transparent?`。Slot：left、right。Events：`back`。

### TabBar 底部标签栏

```vue
<TabBar v-model:value="active" :items="[{key:'home',label:'首页'},{key:'quote',label:'行情'}]" @change="onTab" />
```
Props：`items: { key; label; icon? }[]`、`value?`（v-model，默认 0）。Events：`update:value`、`change`。

### BottomSheet 底部抽屉

```vue
<BottomSheet v-model:open="open" title="高级筛选">
  <Tag>热门题材</Tag>
  <template #footer><Button type="primary">确认</Button></template>
</BottomSheet>
```
Props：`open?`（v-model）、`title?`、`maskClosable?`（默认 true）。Slot：default、footer。Events：`update:open`、`close`。

### ActionSheet 动作面板

```vue
<ActionSheet v-model:open="open" :actions="[{key:'share',label:'分享'},{key:'del',label:'删除',danger:true}]" @select="onSelect" />
```
Props：`open?`（v-model）、`actions: { key; label; danger? }[]`、`cancelText?`（默认"取消"）。Events：`update:open`、`select`、`cancel`。

### GlobalChatBar 全局对话栏

```vue
<GlobalChatBar placeholder="问点什么…" :back="true" @click="openChat" @back="goBack" />
```
Props：`placeholder?`、`back?`、`fixed?`（默认 true）。Slot：default。Events：`click`、`back`。

### PageShell 页面容器

```vue
<PageShell title="股票详情" nav-type="standard" back @back="goBack">
  <template #tabs><PeriodSelector v-model:value="p" /></template>
  <QuoteHeader name="贵州茅台" :price="1680" :change="18.2" :change-pct="1.1" />
  <template #footer><GlobalChatBar /></template>
</PageShell>
```
Props：`title?`、`subtitle?`、`navType?: 'standard'|'transparent'|'white'`、`showNav?`、`back?`、`contentPadding?`。Slot：nav、tabs、default、footer、nav-extra。Events：`back`。

---

## 11. 金融业务深组件

### QuoteHeader 行情头

```vue
<QuoteHeader name="贵州茅台" code="600519" :price="1680" :change="18.2" :change-pct="1.1" tag="沪深300" />
```
Props：`name`、`code?`、`price`、`change`、`changePct`、`tag?`、`status?: 'normal'|'up'|'down'`（默认按 change 符号自动判断）。Slot：default。涨跌色自动应用 $up/$down。

### Ticker 滚动行情条

```vue
<Ticker :items="[{name:'贵州茅台',price:1680,changePct:1.1}]" :speed="25" />
```
Props：`items: { name; price; changePct }[]`、`speed?`（默认 30 秒）。CSS 无缝滚动，hover 暂停。

### AnnounceCard 公告研报卡片

```vue
<AnnounceCard type="research" title="宁德时代市占率分析" source="中信证券" time="2026-08-01" summary="…" @click="onDetail" />
```
Props：`type?: 'announce'|'research'`、`title`、`source?`、`time?`、`summary?`。Slot：default、footer。Events：`click`。

### DataFreshness 数据新鲜度

```vue
<DataFreshness status="realtime" />
<DataFreshness status="delay" delay="15min" />
<DataFreshness status="closed" />
```
Props：`status?: 'realtime'|'delay'|'closed'`、`label?`、`delay?`。

### Heatmap 市场热力图

```vue
<Heatmap :cells="[{name:'白酒',changePct:1.85},{name:'新能源',changePct:-2.15}]" />
```
Props：`cells: { name; changePct }[]`、`cols?`。按涨跌幅度分级着色（红涨绿跌）。

### FundFlow 资金流向

```vue
<FundFlow :flows="[{day:'08-01',main:2.3},{day:'07-31',main:-1.5}]" :inflow="12.34" :outflow="8.76" />
```
Props：`flows: { day; main }[]`（main 正为净流入）、`inflow?`、`outflow?`。

### Watchlist 自选股管理

```vue
<Watchlist :items="list" editable @remove="onRemove" @click="onPick" />
```
Props：`items: { name; code; price; changePct }[]`、`editable?`。Slot：action（scoped `{ item, index }`）。Events：`remove`、`click`。

### RadarChart 雷达图

```vue
<RadarChart :dimensions="[{label:'成长',value:82},{label:'估值',value:45}]" :size="360" status="primary" />
```
Props：`dimensions: { label; value; max? }[]`、`size?`（默认 400 rpx）、`status?: 'primary'|'success'|'warning'|'danger'|'gold'`。SVG 绘制，空数据显示空态。

### RelationGraph 关系图谱

```vue
<RelationGraph :nodes="nodes" :links="links" direction="vertical" />
```
Props：`nodes: { key; label; category?: 'core'|'upstream'|'downstream' }[]`、`links: { from; to; label? }[]`、`direction?: 'vertical'|'horizontal'`。SVG 树状图，空数据显示空态。

---

## 11.5 洞见标志组件

> 洞察之眼系统：全站"洞见"入口统一使用的标志性组件，瞳孔即洞见类型，独立于通用组件库风格。

### InsightTag 洞见标签

```vue
<InsightTag type="emotion">情绪洞见</InsightTag>
<InsightTag type="market" size="lg">市场洞见</InsightTag>
```
Props：`type?: 'emotion'|'fund'|'event'|'market'`（默认 emotion）、`size?: 'sm'|'md'|'lg'`（默认 md）。
瞳孔语义：虚线外环=数据流，虹膜渐变=数据汇聚，瞳孔+高光=AI 看穿本质。瞳孔颜色即类型：情绪红 / 资金金 / 事件青 / 市场蓝。

### InsightCard 洞见卡片

```vue
<InsightCard
  type="emotion"
  title="市场情绪降至冰点"
  trace="跌停家数显著增加，抛压集中释放"
  forecast="情绪回暖概率大，短线或迎反弹窗口"
  time="08-21 · 09:10"
  show-meta
  confidence="0.82"
/>
<!-- 深蓝研报卡 -->
<InsightCard type="market" title="放量上攻" trace="北向资金净流入" forecast="维持强势" theme="dark" show-meta />
```
Props：`type?`、`title`（结论一句话）、`trace`（溯源）、`forecast`（预判）、`time?`、`theme?: 'light'|'dark'`（默认 light）、`showMeta?`、`confidence?`。Events：`click`。
结构规范：瞳孔标签置前 → 结论 → 溯源 → 预判（两句话上限）。

---

## 12. 导航扩展组件

### Breadcrumb 面包屑

```vue
<Breadcrumb :items="[{ label: '首页', path: '/' }, { label: '行情', path: '/market' }, { label: '详情' }]" @click="handleClick" />
```
Props：`items: { label: string; path?: string }[]`、`separator: string = '/'`。最后一项高亮 `$primary`。Event：`click(item)`。

### Divider 分隔线

```vue
<Divider text="更多信息" text-position="center" />
<Divider type="vertical" />
<Divider dashed />
```
Props：`type: 'horizontal'|'vertical' = 'horizontal'`、`dashed: boolean = false`、`text?: string`、`textPosition: 'left'|'center'|'right' = 'center'`。

### Steps 步骤条

```vue
<Steps :steps="[{ title: '填写信息' }, { title: '验证身份' }, { title: '完成' }]" :current="1" />
```
Props：`steps: { title: string; description?: string }[]`、`current: number = 0`。当前步骤 `$primary` 高亮，已完成 `$down` 绿色。

### Segmented 分段控制器

```vue
<Segmented v-model="active" :options="[{ label: '日K', value: 'day' }, { label: '周K', value: 'week' }]" />
```
Props：`options: { label: string; value: string|number }[]`、`modelValue: string|number`。选中项白底 `$primary` 文字。Event：`update:modelValue`。

### Dropdown 下拉菜单

```vue
<Dropdown :items="[{ label: '编辑', value: 'edit' }, { label: '删除', value: 'delete', divided: true }]" @select="handleSelect">
  <template #trigger>点击下拉</template>
</Dropdown>
```
Props：`trigger: 'click'|'hover' = 'click'`、`items: { label; value; disabled?; divided? }[]`、`placement: 'bottom-start'|'bottom-end' = 'bottom-start'`。Event：`select(value)`、`visible-change(visible)`。

---

## 13. AI 交互组件

### StreamingText 流式文本

```vue
<StreamingText text="今日A股市场整体偏强，沪指收涨0.86%" :speed="50" @done="onDone" />
```
Props：`text: string`、`speed: number = 50`(ms/字)、`cursor: boolean = true`、`loop: boolean = false`。光标以 `$primary` 闪烁。Event：`done()`。

### ChatBubble 聊天气泡

```vue
<ChatBubble role="user" content="帮我分析一下今日大盘走势" />
<ChatBubble role="assistant" content="今日沪指收涨0.86%...">
  <template #default><div>富内容卡片</div></template>
</ChatBubble>
```
Props：`role: 'user'|'assistant'`、`content?: string`、`avatar?: string`。user 右对齐 `$primary` 背景白字；assistant 左对齐 `$bg-card` 背景 + AI 头像。Slot：`default`（assistant 富内容）。

### AiEventReport AI 事件报告

```vue
<AiEventReport title="央行降准0.5个百分点" summary="释放长期资金约1万亿元" :points="[{ text: '利好银行板块', color: 'up' }, { text: '地产链预期改善', color: 'primary' }]" />
```
Props：`title: string`、`summary: string`、`points: { text: string; color?: 'up'|'down'|'primary'|'warning' }[]`。左侧 `$primary` 色条 + AI 徽章 + 彩色圆点要点。

### AiGraph AI 关系图谱

```vue
<AiGraph :nodes="[{ label: '上游', type: '硅片' }, { label: '中游', type: '芯片' }]" :links="[{ from: 0, to: 1, type: 'dashed' }]" />
```
Props：`nodes: { label: string; type: string; color?: string }[]`、`links: { from: number; to: number; type?: 'solid'|'dashed'|'curve' }[]`。SVG 连线 + 圆形节点。

---

## 14. 音频与播客组件

### AudioPlayer 音频播放器

```vue
<AudioPlayer title="今日市场早播报" src="/audio/morning.mp3" :duration="62" @play="onPlay" @ended="onEnded" />
```
Props：`title: string`、`src: string`、`duration?: number`(秒)、`cover?: string`、`autoplay: boolean = false`。中央大按钮 `$brand-gradient` + `$shadow-primary`。Event：`play()`、`pause()`、`ended()`、`timeupdate(currentTime)`。

### PodcastCard 播客卡片

```vue
<PodcastCard title="异动个股深度分析" status="idle" @generate="onGenerate" @play="onPlay" @retry="onRetry" />
```
Props：`title: string`、`desc?: string`、`duration?: string`、`status: 'idle'|'loading'|'ready'|'error' = 'idle'`、`cover?: string`。四态切换。Event：`generate()`、`play()`、`retry()`。

### FloatingPodcast 浮动播客

```vue
<FloatingPodcast title="今日市场早播报" :visible="true" :playing="true" :progress="35" @toggle-play="onToggle" @close="onClose" />
```
Props：`title: string`、`visible: boolean = true`、`playing: boolean = false`、`progress: number = 0`(0-100)。固定底部 `$z-fixed`。Event：`toggle-play()`、`close()`、`click()`。

---

## 15. 事件与洞察组件

### ImportanceStars 重要性星级

```vue
<ImportanceStars :value="4" :max="5" size="md" />
```
Props：`value: number`(1-5)、`max: number = 5`、`size: 'sm'|'md'|'lg' = 'md'`。金色实心 `$gold` + 灰色空心 `$ink-faint`。

### EventItemCard 事件列表项

```vue
<EventItemCard type="政策" direction="up" source="央行官网" time="2026-08-09 14:30" :importance="5" title="央行下调存款准备金率0.5个百分点" :industries="[{ name: '银行', direction: 'up' }]" ai-summary="降准释放流动性约1万亿" @follow="onFollow" @analyze="onAnalyze" />
```
Props：`type`、`direction: 'up'|'down'`、`source`、`sourceVerified: boolean = true`、`time`、`importance: number`、`title`、`industries: { name; direction: 'up'|'down'|'neutral' }[]`、`aiSummary?: string`。左侧涨跌色条。Event：`follow()`、`analyze()`。

### EventHeadlineCard 事件头条

```vue
<EventHeadlineCard title="国务院发布新一轮以旧换新政策" source="新华社" time="2小时前" />
```
Props：`title: string`、`source: string`、`time: string`。`$brand-gradient` 渐变背景 + 白色标题。

### InsightListCard 洞察列表

```vue
<InsightListCard title="今日洞察" :items="[{ text: '半导体板块主力净流入45亿', time: '10:32', direction: 'up' }]" />
```
Props：`title: string = '今日洞察'`、`items: { text; time; direction: 'up'|'down'|'neutral' }[]`。彩色圆点 + 文本 + 时间。

### InsightAlertCard 异动提醒

```vue
<InsightAlertCard stock-name="寒武纪" stock-code="688256" direction="up" tag-text="涨停" message="涨幅达9.87%触及涨停" alert-type="涨停异动" confidence="high" time="14:25" />
```
Props：`stockName`、`stockCode`、`direction: 'up'|'down'`、`tagText`、`message`、`alertType`、`confidence: 'high'|'mid'|'low'`、`time`。方向色头部 + 置信度标签。

### GuideCard 引导卡片

```vue
<GuideCard title="欢迎使用 AI 分析" desc="智能分析市场趋势" theme="primary" action-text="开始使用" @action="onAction" />
```
Props：`title`、`desc`、`theme: 'primary'|'accent'|'gold' = 'primary'`、`actionText?: string`。三主题渐变背景。Event：`action()`。

---

## 16. 高级行情组件

### StockChart K线图

```vue
<StockChart name="寒武纪" code="688256" :price="256.80" :change="23.10" :change-pct="9.87" :candles="candleData" :ma-lines="maData" @period-change="onPeriod" />
```
Props：`name`、`code`、`price: number`、`change: number`、`changePct: number`、`candles: { open; close; high; low; volume; isUp: boolean }[]`、`periods?: string[]`、`maLines?: { name; color; data: number[] }[]`。红涨绿跌蜡烛图 + 均线 + 成交量。Event：`period-change(period)`。

### TrendKlineCompare K线对比

```vue
<TrendKlineCompare :legends="[{ name: '个股', color: '#0b5fff' }, { name: '行业均值', color: '#c89020' }]" :series="[{ points: '0,60 100,30', color: '#0b5fff' }]" />
```
Props：`legends: { name; color }[]`、`series: { points: string; color: string; dash?: boolean }[]`。SVG 多线对比。

### TrendDimCard 趋势维度

```vue
<TrendDimCard title="资金面" :score="85" desc="主力净流入45亿，北向连续3日加仓" />
```
Props：`title: string`、`score: number`(0-100)、`desc: string`。>70 `$up`，40-70 `$primary`，<40 `$warning`。

### StockDetailTable 股票详情表格

```vue
<StockDetailTable :columns="[{ key: 'name', title: '名称' }, { key: 'changePct', title: '涨跌' }]" :data="stockData" up-down-field="changePct" />
```
Props：`columns: { key; title; width? }[]`、`data: Record<string, string|number>[]`、`upDownField?: string`。涨红跌绿。

### StockCardList 股票卡片列表

```vue
<StockCardList :stocks="[{ name: '北方华创', code: '002371', industry: '半导体', price: 312.50, changePct: 8.20 }]" />
```
Props：`stocks: { name; code; industry?; price: number; changePct: number }[]`。卡片式列表。

### StockIntelList 股票情报列表

```vue
<StockIntelList :items="[{ title: '订单饱满·北方华创', desc: 'Q3订单同比增长45%', type: 'info' }]" />
```
Props：`items: { title; desc; type: 'info'|'up'|'warning' }[]`。图标圆 + 标题 + 描述。

### WindLeaderPanel 风口龙头面板

```vue
<WindLeaderPanel :tabs="['长线风口', '短线风口']" active-tab="长线风口" :bubbles="bubbleData" @tab-change="onTab" />
```
Props：`tabs: string[]`、`activeTab: string`、`bubbles: { label; sublabel?; size; color; x; y }[]`。SVG 泡泡图，长线蓝色梯度，短线金色梯度。Event：`tab-change(tab)`。

---

## 17. 辅助组件

### LoginQrCode 扫码登录

```vue
<LoginQrCode title="扫码登录" desc="使用 App 扫描二维码登录" :expired="false" @refresh="onRefresh" />
```
Props：`title: string = '扫码登录'`、`desc: string`、`expired: boolean = false`、`qrData?: string`。支持图片渲染或 CSS 模拟二维码。Event：`refresh()`。

### Analytics 使用分析

```vue
<Analytics title="使用分析" period="近7天" :items="[{ label: '查看次数', value: 1238, trend: 12.5, trendDirection: 'up' }]" />
```
Props：`title`、`period: string = '近7天'`、`items: { label; value; trend?; trendDirection?: 'up'|'down' }[]`。2x2 网格 + 趋势色标。

### CycleSelect 周期选择器

```vue
<CycleSelect v-model="period" :options="[{ label: '1日', value: '1d' }, { label: '5日', value: '5d' }]" />
```
Props：`options: { label; value }[]`、`modelValue: string|number`。`$bg-soft` 背景 + 选中白底 `$primary`。Event：`update:modelValue`。

### NewsSlider 新闻轮播

```vue
<NewsSlider :news="[{ tag: '快讯', title: '央行降准0.5个百分点', source: '新华社' }]" :interval="4000" @change="onChange" @click="onClick" />
```
Props：`news: { tag; title; source }[]`、`interval: number = 4000`、`visible: boolean = true`。`$brand-gradient` 背景 + 自动轮播。Event：`change(index)`、`click(news)`。

### HotBurstPanel 热点爆发

```vue
<HotBurstPanel time="14:25" :items="[{ name: '半导体', pct: 3.2 }]" />
```
Props：`time: string`、`items: { name; pct: number }[]`。红色基调 `$up-bg` + 闪电图标。

### MarketOverview 大盘概览

```vue
<MarketOverview status="实时" refresh-text="60s 刷新" :indices="indexData" />
```
Props：`status: string = '实时'`、`refreshText: string = '60s 刷新'`、`indices: { name; code; price; changeAmt; changePct; volume; sparkline }[]`。6 指数网格 + 迷你走势图 + 涨跌色条。

---

## 18. Design Token 变量速查

### 品牌色

| 变量 | 色值 | 用途 |
|------|------|------|
| `$primary` | `#0b5fff` | 主品牌色 |
| `$primary-deep` | `#0a2e6f` | 深品牌色 |
| `$primary-light` | `#4d8bff` | 浅品牌色 |
| `$primary-50` | `#eaf2ff` | 最浅背景 |
| `$accent` | `#0b5fff` | 蓝色强调（与 primary 同色） |
| `$gold` | `#c89020` | 金色高级 |
| `$gold-deep` | `#8a6411` | 深金色 |

### 语义色（A 股红涨绿跌）

| 变量 | 色值 | 用途 |
|------|------|------|
| `$up` | `#e54d5e` | 上涨（红） |
| `$down` | `#18a058` | 下跌（绿） |
| `$flat` | `#909399` | 平盘 |
| `$warning` | `#c89020` | 警告 |

### 洞察类型色（洞察之眼）

| 变量 | 色值 | 用途 |
|------|------|------|
| `$insight-emotion` / `-light` / `-deep` | 红系 | 情绪洞见（瞳孔红） |
| `$insight-fund` / `-light` / `-deep` | 金系 | 资金洞见（瞳孔金） |
| `$insight-event` / `-light` / `-deep` | 青系 | 事件洞见（瞳孔青） |
| `$insight-market` / `-light` / `-deep` | 蓝系 | 市场洞见（瞳孔蓝） |
| `$insight-card-dark-gradient` | 深蓝渐变 | 深蓝研报卡背景 |

### 文字色

| 变量 | 色值 | 用途 |
|------|------|------|
| `$ink` | `#0a1733` | 主文字 |
| `$ink-soft` | `#4b5a7a` | 次文字 |
| `$ink-mute` | `#8a96b0` | 辅助文字 |
| `$ink-faint` | `#b8c3d6` | 占位文字 |

### 圆角

| 变量 | 值 | 用途 |
|------|-----|------|
| `$r-xs` | `8rpx` | 小圆角（标签） |
| `$r-sm` | `12rpx` | 小圆角 |
| `$r-md` | `16rpx` | 中圆角（输入框） |
| `$r-lg` | `20rpx` | 大圆角（卡片） |
| `$r-xl` | `24rpx` | 超大圆角 |
| `$r-full` | `9999rpx` | 全圆角（胶囊） |

### 间距

| 变量 | 值 | 用途 |
|------|-----|------|
| `$s-1` | `8rpx` | 最小间距 |
| `$s-2` | `16rpx` | 小间距 |
| `$s-3` | `24rpx` | 中间距 |
| `$s-4` | `32rpx` | 标准间距 |
| `$s-5` | `40rpx` | 大间距 |
| `$s-6` | `48rpx` | 超大间距 |

### 阴影

| 变量 | 用途 |
|------|------|
| `$shadow-xs` | 微阴影（卡片默认） |
| `$shadow-sm` | 小阴影 |
| `$shadow-card` | 卡片阴影 |
| `$shadow-hover` | 悬浮阴影 |
| `$shadow-glow` | 聚焦光环 |
| `$shadow-primary` | 品牌色阴影 |

---

## 19. 常见问题

### Q: 如何在 uni-app 项目中使用？

组件已按 uni-app 规范编写（view/text/image/scroll-view 标签 + rpx 单位），直接安装引用即可。

### Q: A 股涨跌色怎么用？

```scss
// 上涨用红色
color: $up;     // #e54d5e

// 下跌用绿色
color: $down;   // #18a058
```

### Q: 如何自定义主题色？

修改 `src/styles/variables.scss` 中的 `$primary` 变量即可，所有组件自动生效。

### Q: 可以只引入部分组件吗？

可以，按需 import 即可：

```ts
import { Button, Card } from 'aistock-component-lib'
```

### Q: 组件支持暗色模式吗？

当前版本为蓝白浅色主题，暗色模式将在后续版本支持。

---

*本指南随组件库迭代持续更新。如有问题请联系林晓研或尹辰。*
